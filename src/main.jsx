import React from "react";
import ReactDOM from "react-dom/client";
import BishopLandingPage from "../bishop-landing-page.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BishopLandingPage />
  </React.StrictMode>
);
